这是一个更简单的点灯程序，通常情况下这个点灯程序能够用于不同的STM32芯片；

这个程序与0_led的区别在于，这个程序不需要使用到动态内存，所以系统堆(heap)是关闭的。

Note: 
因为系统不能使用动态内存，所以所有rt_xx_create类似的函数将不可使用（因为它们都会从动态内存中分配内存）。

在application.c用户程序中，因为rt_thread_create已经不可使用，所以采用了 rt_thread_init 函数
初始化一个静态的线程（线程的TCB、栈都在编译时由全局变量静态分配出内存）。